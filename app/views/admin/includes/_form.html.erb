<%= form_for [:admin, @site, @include], :html => {:class => "full"} do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <div class="form_actions">
    <%= f.submit :class => "btn btn-success" %>
  </div>
<% end %>
