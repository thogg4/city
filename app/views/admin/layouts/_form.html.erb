<%= form_for [:admin, @site, @layout], :html => {:class => "full"} do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_area :body %>
  <p class="help-block">
    Use {{ page }} to show where the page should be placed
  </p>

  <div class="form-actions">
    <%= f.submit :class => "btn btn-success" %>
  </div>

<% end %>
